<h1> All tasks </h1>

<ul>
  <% @tasks.each do |task| %>
    <li> <%= link_to task.title, task_path(task)%> <%= link_to "Delete" ,  task_path(task), data: { turbo_method: 'delete', turbo_confirm: 'Are you sure?' }  %></li>
<li><%= link_to "Edit a ", task_edit_path(task) %></li>
  <%end %>

</ul>

<%= link_to "Add new", tasks_new_path %>
